{
  "name": "azure-storage-blob-samples-ts",
  "private": true,
  "version": "0.0.1",
  "main": "index.js",
  "description": "Azure Storage Blob client library samples for TypeScript",
  "engine": {
    "node": ">=8.0.0"
  },
  "scripts": {
    "build": "tsc",
    "prebuild": "rimraf dist/",
    "dev": "tsc --watch & nodemon dist",
    "prod": "rimraf dist/ && tsc && node dist/index.js"
  },
  "sideEffects": false,
  "dependencies": {
    "express": "4.15.3",
    "nodemon": "1.11.0",
    "@azure/abort-controller": "latest",
    "@azure/identity": "latest",
    "@azure/storage-blob": "latest",
    "build": "^0.1.4",
    "dotenv": "^8.2.0",
    "run": "^1.4.0"
  },
  "devDependencies": {
    "@types/node": "^8.0.0",
    "rimraf": "^3.0.0",
    "typescript": "~3.6.4"
  }
}
